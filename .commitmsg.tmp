fix(openapi): 开放平台管理类写操作收归管理员，堵住免登录缺口（P0）

问题：require_auth 白名单用 "/openapi/" 模糊匹配，导致
POST /openapi/apps（注册应用）、/keys（签发密钥）、/keys/{id}/revoke、
/webhooks、/webhooks/{id}/test 全部免登录可调用——任何人拿到租户
code 即可为自己签发 API Key 并注册回调地址，是完整的越权接管路径。

修法：
- 白名单由 "/openapi/" 收窄为 "/openapi/v1/"（第三方只读接口）
  + path.endswith("/openapi/verify-key")（Key 校验端点本身）。
  后者仍需 API-Key，不与登录会话挂钩，语义不变。
- 5 个管理类写操作加 dependencies=[Security(require_perm,
  scopes=[USER_MANAGE])]。USER_MANAGE 为管理员专有点（门店经理
  权限列表无此项），复用即实现"仅管理员"，无需新增权限点。
- 前端 OpenApi.tsx 同步加 useCan(PERM.USER_MANAGE) 门控：
  注册应用 / 生成新密钥 / 吊销 三处按钮 disabled，
  未授权时操作列降级为"仅管理员可管理"，避免"点了报 403"。

验证：
- tests/test_auth_enforcement.py 16 passed（原 9 + 新 7），
  新增 TestOpenApiAdminOnly：匿名注册应用现返回 401（此前 201）。
- 门控台账首次真正偿债：unprotected_writes 89 -> 84（-5），
  protected 65 -> 70，无新增欠债。
- test_route_perm_guard.py 2 passed；全量回归 493 passed。
- tsc --noEmit 0 错；vite build ✓；雪花 ID 哨兵 PASS。
